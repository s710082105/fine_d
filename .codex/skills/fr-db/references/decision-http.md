# Decision HTTP Scope

- Login payload uses `username`, `password`, `validity`, `sliderToken`, `origin`, `encrypted`
- Connection list endpoint: `/v10/config/connection/list/0`
- Data preview uses Decision HTTP, not the bridge jar
- SQL and field evidence must be captured before CPT/FVS editing
