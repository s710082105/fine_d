# browser-validate

Validate generated report behavior through browser-based checks.

## Required Behavior

- Use runtime context files before validation.
- Report mismatches with concrete evidence.
- Never mask validation failures.
