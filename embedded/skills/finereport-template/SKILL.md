# finereport-template

Generate and modify FineReport templates with strict path mapping awareness.

## Required Behavior

- Follow `mappings.json` source-target mappings.
- Keep generated outputs inside mapped local source paths.
- Surface rendering or mapping failures directly.
